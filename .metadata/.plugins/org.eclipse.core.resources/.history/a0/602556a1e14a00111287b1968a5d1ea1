import com.settinghead.wexpression.Word;

class BootStrap {

    def init = { servletContext ->
		new Word(word:"quick", weight:10).save()
		new Word(word:"brown", weight:9).save()
		new Word(word:"fox", weight:10).save()
    }
    def destroy = {
    }
}
